<div class="row">
	<% if current_user.admin %>
		<h3 class="text-center">Create a new home</h3>
    <hr>
		<div class="col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
			<%= render 'form' %>
		</div>
	<% else %>
		<h2 class="center">Only admins have access to this feature</h2>
	<% end %>
</div>
