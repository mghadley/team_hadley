<div class="container">
  <div class="col-xs-10 col-xs-offset-1">
  	<% if current_user && current_user.admin %>
  		<%= link_to "New blog post", new_post_path %>
  	<% end %>
  		<%= react_component 'Posts', { posts: @posts, admin: admin? } %>
  </div>
</div>
